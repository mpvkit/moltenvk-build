name: CI Test

on:
  push:
    branches:
      - ci
  workflow_dispatch:
    inputs:
      version:
        description: 'Release tag name (default vulkan version)'
        default: ''
        type: string

jobs:
  build:
    permissions:
      contents: write
    uses: mpvkit/BuildShared/.github/workflows/build.yml@9c51172cc0fe59112866090f4f307f73c4bc27e3
    secrets: inherit
    with:
      version: '${{ github.event.inputs.version }}'
      library: 'vulkan'
      # Reuse the shared release workflow without pushing Package.swift or creating a GitHub release.
      try_run: true
